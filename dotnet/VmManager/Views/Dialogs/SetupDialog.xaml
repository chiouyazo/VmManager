<Window
  x:Class="VmManager.Views.Dialogs.SetupDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Welcome to VM Manager"
  Width="520"
  SizeToContent="Height"
  ResizeMode="NoResize"
  WindowStartupLocation="CenterScreen"
  Background="White"
>
  <Grid Margin="32,28,32,28">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <TextBlock
      Grid.Row="0"
      Text="Connect to your Registry"
      FontSize="20"
      FontWeight="SemiBold"
      Foreground="#1A1A1A"
      Margin="0,0,0,8"
    />
    <TextBlock
      Grid.Row="1"
      Text="VM Manager pulls box images from an OCI registry (Zot). Enter your registry URL to get started."
      Foreground="#6B6B6B"
      TextWrapping="Wrap"
      FontSize="13"
      Margin="0,0,0,24"
    />

    <!-- Registry URL + Load button -->
    <TextBlock Grid.Row="2" Text="Registry URL" Foreground="#1A1A1A" Margin="0,0,0,4" />
    <Grid Grid.Row="3" Margin="0,0,0,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox
        Grid.Column="0"
        x:Name="RegistryUrlBox"
        FontSize="14"
        Margin="0,0,8,0"
        ToolTip="e.g. http://10.5.1.15:5000"
      />
      <Button
        Grid.Column="1"
        x:Name="LoadReposButton"
        Content="Load Repos"
        Padding="14,6"
        Background="#C8102E"
        Foreground="White"
        Click="LoadRepos_Click"
      />
    </Grid>

    <!-- Repository dropdown -->
    <TextBlock Grid.Row="4" Text="Repository" Foreground="#1A1A1A" Margin="0,0,0,4" />
    <ComboBox
      Grid.Row="5"
      x:Name="RepositoryCombo"
      FontSize="14"
      Margin="0,0,0,8"
      IsEditable="True"
      ToolTip="Select a repository or type one"
    />

    <!-- Status text -->
    <TextBlock
      Grid.Row="6"
      x:Name="StatusText"
      Foreground="#6B6B6B"
      FontSize="12"
      Margin="0,0,0,20"
    />

    <!-- Buttons -->
    <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button
        Content="Skip for now"
        Padding="16,8"
        Background="#E8E8E8"
        Foreground="#1A1A1A"
        Click="Skip_Click"
        Margin="0,0,12,0"
      />
      <Button
        Content="Connect"
        Padding="24,8"
        Background="#C8102E"
        Foreground="White"
        FontWeight="SemiBold"
        Click="Connect_Click"
      />
    </StackPanel>
  </Grid>
</Window>
